# The instructions for the first stage
#FROM node:10-alpine
FROM scratch

#ARG NODE_ENV=development
#ENV NODE_ENV=${NODE_ENV}

#RUN apk update
#RUN apk --no-cache add bash python make g++


RUN echo "WORKDIR"
RUN echo ${DBUILDER_WORKDIR}
RUN ls -al

#RUN npm install -g http-server 
WORKDIR ${DBUILDER_WORKDIR}

RUN echo -n "CURRENT DIRECTORY:"
RUN pwd

# install dependencies? 

RUN echo "TESTING NEW SECOND BUILD"
RUN ls -al

CMD [ "npm", "run", "second:build" ]
